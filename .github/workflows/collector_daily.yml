name: 01 메인 일일 수집 | 네이버 광고 성과
run-name: 01 메인 일일 수집 | ${{ github.event_name == 'workflow_dispatch' && format('manual | {0} | fast={1}', github.event.inputs.collect_mode, github.event.inputs.fast) || 'schedule' }}

on:
  schedule:
    - cron: "7 19,1,7 * * *"   # KST 04:07 / 10:07 / 16:07
  workflow_dispatch:
    inputs:
      collect_mode:
        description: '수집 모드'
        required: false
        default: '검색광고 전체+기기'
        type: choice
        options:
          - '검색광고 전체+기기'
          - '검색광고 전체만'
          - '기기만'
      fast:
        description: '수동 실행 시 빠른 수집 사용 여부'
        required: false
        default: '예'
        type: choice
        options:
          - '예'
          - '아니오'

concurrency:
  group: collector-daily
  cancel-in-progress: false

jobs:
  run-collector:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Seoul
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      NAVER_ADS_API_KEY: ${{ secrets.NAVER_ADS_API_KEY }}
      NAVER_ADS_SECRET: ${{ secrets.NAVER_ADS_SECRET }}
      ACCOUNT_MASTER_FILE: account_master.xlsx

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install requests python-dotenv sqlalchemy psycopg2-binary pandas openpyxl


      - name: Write workflow guide
        if: always()
        run: |
          {
            echo "## 01 메인 일일 수집"
            echo
            echo "- 용도: 매일 전일/당일 성과를 자동 갱신하는 본선 워크플로우"
            echo "- 수동 실행 시 모드: `${{ github.event.inputs.collect_mode || '검색광고 전체+기기' }}`"
            echo "- 수동 실행 시 fast: `${{ github.event.inputs.fast || '예' }}`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Run collector (Yesterday / Today optimized)
        env:
          DISPATCH_COLLECT_MODE: ${{ github.event.inputs.collect_mode }}
          DISPATCH_FAST: ${{ github.event.inputs.fast }}
        run: |
          echo "⏰ 현재 한국 시간: $(date +%F' '%H:%M:%S)"

          KST_HOUR=$(date +%H)
          YESTERDAY=$(date -d "yesterday" +%F)
          TODAY=$(date +%F)
          COLLECT_MODE="${DISPATCH_COLLECT_MODE:-검색광고 전체+기기}"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${DISPATCH_FAST}" = "아니오" ]; then
              FAST_FLAG=""
            else
              FAST_FLAG="--fast"
            fi

            echo "🚀 수동 수집 날짜(전일) = ${YESTERDAY} | 모드=${COLLECT_MODE} | 빠른 수집=${DISPATCH_FAST:-예}"
            python collector.py --date "${YESTERDAY}" --workers 2 --collect_mode "${COLLECT_MODE}" ${FAST_FLAG}
          else
            if [ "$KST_HOUR" = "04" ]; then
              echo "🚀 수집 날짜(전일, 전체 갱신) = ${YESTERDAY} | 모드=${COLLECT_MODE}"
              python collector.py --date "${YESTERDAY}" --workers 2 --collect_mode "${COLLECT_MODE}"
            else
              echo "🚀 수집 날짜(전일, 백필형 안전 갱신) = ${YESTERDAY} | 모드=${COLLECT_MODE}"
              python fast_backfill.py --start "${YESTERDAY}" --end "${YESTERDAY}" --workers 2 --collect_mode "${COLLECT_MODE}" --sync_dim_first_day
            fi

            if [ "${COLLECT_MODE}" = "기기만" ] || [ "${COLLECT_MODE}" = "device_only" ]; then
              echo "⏭️ 당일 빠른 수집은 '기기만' 모드에서 생략합니다."
            else
              echo "🚀 수집 날짜(당일, 빠른 갱신) = ${TODAY} | 모드=${COLLECT_MODE}"
              python collector.py --date "${TODAY}" --workers 2 --collect_mode "${COLLECT_MODE}" --fast
            fi
          fi
