name: 06 자동 OFF 점검
run-name: 06 자동 OFF 점검 | ${{ github.event_name == 'workflow_dispatch' && 'manual' || 'hourly' }}

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run-hourly-check:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Seoul
      ACCOUNT_MASTER_FILE: account_master.xlsx

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install psycopg2-binary SQLAlchemy pandas requests python-dotenv openpyxl


      - name: Write workflow guide
        if: always()
        run: |
          {
            echo "## 06 자동 OFF 점검"
            echo
            echo "- 용도: 시간 단위 자동 점검 / OFF 상태 탐지"
            echo "- 참고: 성과 수집이 아니라 점검 성격의 워크플로우"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Run OFF Scanner
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NAVER_ADS_API_KEY: ${{ secrets.NAVER_ADS_API_KEY }}
          NAVER_ADS_SECRET: ${{ secrets.NAVER_ADS_SECRET }}
          CUSTOMER_ID: ${{ secrets.CUSTOMER_ID }}
        run: |
          python check_off.py
